input {
  file {
    # file
    path => "/tmp/src/data/data.csv"
    # position
    start_position => "beginning"
  }
}


filter {
  csv {
      separator => ","
      columns => ["Date","Open","High","Low","Close","Volume","Adj Close"]
      convert => {
        "Date"   => "date"
        "Open"   => "float"
        "High"   => "float"
        "Low"    => "float"
        "Close"  => "float"
        "Volume" => "float"
      }  
  }
}
